<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="rectangle"
            >

            <!-- Button Color -->
            <gradient
                android:angle="90"
                android:startColor="@color/colorLightRedDanger"
                android:centerColor="@color/colorRedDanger"
                android:endColor="@color/colorLightRedDanger"
                />

            <!-- Button Borders -->
            <stroke
                android:width="2dp"
                android:color="@color/colorRedDanger"
                />

            <!-- Button Corners -->
            <corners
                android:radius="50dp"
                />
        </shape>
    </item>

</selector>